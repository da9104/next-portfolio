/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export',
    // distDir: 'dist',
    // basePath: "/next-portfolio",
    // reactStrictMode: true,
};

export default nextConfig;
